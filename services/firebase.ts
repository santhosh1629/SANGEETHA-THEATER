
// MOCK FIREBASE CONFIGURATION
// This file replaces the actual Firebase SDK initialization.
// The app now runs entirely on LocalStorage/Mock Data.

export const auth = {};
export const db = {};
export const storage = {};

console.log("Backend removed. App is running in Local Mock Mode.");
